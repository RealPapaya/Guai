{
  "_comment": "Shape only. Copy to state/secrets.json (gitignored) OR set as env vars. Env vars WIN over the file. Gmail/Calendar use the Claude Code MCP OAuth handshake (no secret here).",
  "GITHUB_TOKEN": "github_pat_xxx — fine-grained PAT, read-only on the repos you watch"
}
